# API backend — FastAPI récent (starlette>=0.49 via dépendance transitive, compatible MCP/sse-starlette)
# Ne pas pinner starlette : fastapi gère la plage compatible (ex. >=0.46,<0.51 sur 0.135+)
fastapi>=0.135.0
uvicorn[standard]>=0.30.0
anyio>=4.7.0
httpx>=0.27.0
python-multipart>=0.0.9
pydantic>=2.7.0,<3
